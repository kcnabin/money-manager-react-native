import { View, Pressable, Pill } from "react-native";
import { mainStyle } from "../../../mainStyle";

const TransactionTypeSelector = ({ handleTransactionTypeSelection }) => {
  return <View style={mainStyle.flexRow}></View>;
};

export default TransactionTypeSelector;
