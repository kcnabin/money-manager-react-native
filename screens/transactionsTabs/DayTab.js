import { View, Text } from "react-native";
import { mainStyle } from "../../mainStyle";

const DayTab = () => {
  return (
    <View style={mainStyle.fullArea}>
      <Text style={mainStyle.bigFont}>DayTab</Text>
    </View>
  );
};

export default DayTab;
