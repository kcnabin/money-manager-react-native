import { View, Text } from "react-native";
import { mainStyle } from "../mainStyle";

const SummaryScreen = () => {
  return (
    <View style={mainStyle.fullArea}>
      <Text style={mainStyle.bigFont}>SummaryScreen</Text>
    </View>
  );
};

export default SummaryScreen;
