import { View, Text } from "react-native";
import { mainStyle } from "../../../mainStyle";

const CalendarTab = () => {
  return (
    <View style={mainStyle.fullArea}>
      <Text style={mainStyle.bigFont}>CalendarTab</Text>
    </View>
  );
};

export default CalendarTab;
